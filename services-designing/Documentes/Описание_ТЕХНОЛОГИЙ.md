- **API Gateway**: Единая точка входа для мобильных приложений (для водителей) и веб-интерфейсов.
  
- **Event-Driven Architecture (EDA)**: Использование брокеров сообщений - **Apache Kafka**) для обмена данными между сервисами (обновление статуса доставки мгновенно уведомляет сервис биллинга).
  
- **Saga Pattern**: Обеспечение целостности данных при выполнении распределенных транзакций (например, бронирование груза на нескольких этапах перевозки).

---
### **Технический стек**

- **Java 17 + Spring Boot 3.5**
- Spring Web    
- **Spring Security**    
- **Keycloak** *(отдельный контейнер)*
- **Spring Data JPA**    
- **PostgreSQL** *(почти в каждом сервисе)*
- **MongoDB** *(UserSevice -для аватаров и документов)*
- **Redis** *(кэш к некоторых сервисах)*
- **Liquibase** *(миграции БД)*
- **Kafka**  *(асинхронно/ для событий)*  
- **REST** *(Feign, синхронно)*    
- **Maven** *(сборка)*    
- **Lombok** — *(убираю бойлер-код)*    
- **Swagger**  — *(документация)*    
- **JUnit 5 + Testcontainers + Mockito** — *(тесты)*
- **Docker + Docker Compose** *(всё запускается одной командой)*
- **Loki + Promtail + Grafana** *(для логов)*
- **Prometheus + Grafana** *(для метрик)*
- ~~**Jaeger**~~ (*для трэйсов*) — но пока реализовывать не буду. 
	  Потом возможно все заменю на **Grafana Alloy** — *(единый агент для метрик, логов и трейсов.)*

---

